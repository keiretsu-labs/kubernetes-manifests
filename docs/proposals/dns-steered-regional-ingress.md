# Proposal: DNS-steered regional ingress

Status: draft for architecture review. This PR includes the low-risk YAML normalization and TSFlow route convergence described below. It does not alter live DNS records or reconcile clusters.

## Decision

Keep one Envoy Gateway fleet and one local LoadBalancer Service per ingress zone in each region, and use DNS as the global steering layer:

- Ottawa, Robbinsdale, and St. Petersburg each keep their public, private, and Tailscale Envoy entry points.
- Public names are steered through the existing k8gb/CDN DNS plane. A shared hostname represents the service, while its regional records point at regional public LoadBalancer VIPs.
- Private names remain on the private/internal DNS plane. They must not depend on public DNS or Tailscale CGNAT addresses.
- Tailscale clients continue to resolve through split DNS and retain the three regional Tailscale Envoy endpoint names as the implementation targets. This is option 1: a shared application name with regional Tailscale edges, not one new shared Tailscale Service.
- Cilium ClusterMesh and the Multi-Cluster Services API remain the east-west data plane. DNS chooses an edge; Envoy and `ServiceImport` choose the backend.
- k8gb resources use the current canonical `k8gb.io/v1beta1` API group. The running k8gb release still serves the legacy group for migration, but new GitOps objects should use the canonical group.
- There is no Cloudflare Load Balancer, BGP anycast, or cross-region L2 LoadBalancer in this design.
- The legacy shared tailnet hostname remains retired. The regional `*.keiretsu.ts.net` names are endpoint implementation details, not user-facing application URLs.

The important distinction is that “shared LoadBalancer” means a shared DNS name and policy, not one IP stretched across three regions. Each region still owns a local VIP and can fail independently.

## Current and target request flow

```text
                         DNS steering
       public DNS  ─────────────────────────────┐
       private DNS ─────────────────────────────┤
       tailnet split DNS ──────────────────────┤
                                                ▼
        regional public/private/Tailscale Envoy Gateway
                              │
                    Gateway API HTTPRoute
                              │
              ┌───────────────┴────────────────┐
              │                                │
       local Service                    ServiceImport
       (regional-only)                  (MCS shared app)
                                               │
                               Cilium ClusterMesh east-west
                                               │
                       ServiceExport in each serving region
```

The regional edge remains the failure boundary for ingress. A DNS answer can move a client to another healthy regional edge, while a multi-region route can still select a healthy backend through MCS. These are separate decisions and should not be conflated.

## Regional ingress inventory

| Zone | Regional edge | Target of DNS steering | Client scope |
| --- | --- | --- | --- |
| Public | `envoy-home-public-*` | Regional public LoadBalancer VIP | Internet/public DNS |
| Private | `envoy-home-private-*` | Regional private LoadBalancer VIP | Private/internal DNS |
| Tailscale | `envoy-home-ts-*` | `<location>-home-envoy-gateway.keiretsu.ts.net` | Tailscale split DNS |

The service suffixes are generated by Envoy Gateway and must continue to be discovered from the live `Gateway`/`Service` objects rather than copied into application manifests. The Tailscale endpoint names are intentionally preserved for option 1 so this proposal does not silently change tailnet access policy.

## Standard HTTPRoute and MCS contract

Every application exposed in more than one region should have the same route contract in every serving cluster:

1. Use the same user-facing hostname and path in every region. The application hostname identifies the app; `/` is the canonical entry path unless the app explicitly owns a different prefix.
2. Do not encode a region in the public, private, or Tailscale path. Region selection belongs to DNS and MCS.
3. Use explicit `parentRefs` with `sectionName` for the intended `public`, `private`, or `ts` listener. Do not rely on an implicit listener match.
4. Use a normal `Service` for a regional-only backend. Use a same-named `ServiceImport` for a backend deliberately served through MCS.
5. Create the matching `ServiceExport` in every region that serves the application. Keep namespace, service name, port, and route path identical across regions.
6. Keep Envoy Gateway on `routingType: Service` for these routes. Cilium provides the cross-cluster service path; Envoy should not be made responsible for discovering remote pod addresses.
7. Keep public, private, and Tailscale exposure as separate `HTTPRoute` parent relationships. One route may attach to multiple gateways only when the exposure is intentional and documented.

This makes `tsflow.rajsingh.info` and `mcsapi` examples of the same pattern rather than special cases: one canonical route shape, one MCS service identity, and three regional deployment decisions.

This PR implements the TSFlow part of that contract in
`kubernetes/apps/base/tailscale-examples/tsflow-mcs/httproute.yaml`. The
route-only base is now deployed in all three location trees. The TSFlow
workload remains deployed only where its producer and storage contract exists;
the other regional edges route to its MCS `ServiceImport`. The route is
attached to the public and Tailscale gateways with the same three hostnames and
the `/` path in every region. It is not attached to the private gateway.

## DNS planes and health behavior

### Public

k8gb remains the public GSLB authority for names that are intentionally multi-region. The public record chain should terminate at regional public Envoy VIPs through the existing `cdn.keiretsu.top` structure. A hostname should be placed in this pool only after the same route and backend contract exists in all intended regions.

Single-region services may continue to use an owner-pinned record. That is an explicit availability choice, not a failed attempt at multi-region routing.

### Private

Private DNS remains separate from public DNS. Multi-region private names should use the same regional steering policy where required, but the records and health checks must remain internal. No private route should be made reachable merely because a public `HTTPRoute` exists.

### Tailscale

The current split-DNS path sends the relevant zones to the regional k8gb CoreDNS services. The current Corefile still contains owner-pinned CNAME answers, for example one suffix resolving to one regional Tailscale Envoy endpoint. That is deterministic routing, not health-aware failover.

Before changing those answers, we need one approved tailnet-only health mechanism:

- a private/tailnet-only GSLB view that can publish health-eligible regional targets; or
- a controller-driven Corefile/record update that removes unhealthy regional targets and has a defined TTL and recovery policy.

The implementation must never publish `100.64.0.0/10` Tailscale addresses or `.ts.net` endpoint targets in public DNS. It must also preserve the existing Tailscale service and ACL contract until the replacement has been tested from tailnet clients. Simply returning all three current `.ts.net` names is not sufficient, because that would be round-robin distribution without endpoint health gating.

## Consolidation and cleanup

The target consolidation is:

- one documented ingress contract for all three regions;
- one k8gb public steering plane instead of application-specific DNS behavior;
- one private DNS policy for internal names;
- one tailnet split-DNS policy that delegates to the approved tailnet-only health mechanism;
- regional Envoy deployments retained as interchangeable edges;
- Cilium ClusterMesh/MCS retained as the shared east-west service fabric;
- the legacy DNS forwarder removed from the authoritative request path and any remaining references inventoried before deletion;
- legacy shared tailnet hostname references removed from routes, DNS records, documentation, tests, and Tailscale policy;
- duplicate or owner-specific HTTPRoute definitions collapsed where the application is truly replicated in all three regions.

The consolidation must not merge public, private, and tailnet trust boundaries. It removes duplicated policy and naming, not the security boundaries themselves.

The executable portion of this PR is intentionally small:

- all checked-in k8gb `Gslb` objects now use `k8gb.io/v1beta1`;
- the TSFlow HTTPRoute lives in one route-only MCS base and is deployed in all
  three regions;
- the old per-workload route copy is removed from the TSFlow workload base;
- the generated deployment inventory reflects the two new regional Flux
  pointers.

## Proposed rollout

1. Inventory every `Gateway`, `HTTPRoute`, `ServiceExport`, `ServiceImport`, k8gb `Gslb`, CNAME, legacy DNS forwarder reference, and legacy shared tailnet hostname reference.
2. Classify each application as regional-only, multi-region public, multi-region private, or tailnet-only.
3. Normalize routes and MCS identities for applications classified as multi-region. Validate the complete hostname/path matrix from each regional edge.
4. Choose and implement the tailnet-only health-aware DNS mechanism. Test resolver caching, TTL expiry, regional outage, recovery, and split-DNS behavior before changing owner-pinned answers.
5. Move only replicated public names into the k8gb multi-region pool. Leave regional-only names owner-pinned until replication is real.
6. Remove the legacy DNS forwarder from remaining clients and authorities, then remove its manifests and documentation references in a separate reviewable cleanup commit.
7. Remove the legacy shared tailnet hostname surface only after reference and live DNS sweeps are empty. Keep the regional `.keiretsu.ts.net` endpoint names where option 1 requires them.

## Acceptance criteria

- The repository render gate passes for all three clusters.
- Every intended route reports an admitted parent and an available backend; no route relies on an accidental listener match.
- A replicated application has the same hostname, path, service identity, and port in Ottawa, Robbinsdale, and St. Petersburg.
- Public DNS contains no Tailscale CGNAT address or `.ts.net` target.
- Private and tailnet DNS remain inaccessible from the wrong client population.
- A regional public edge outage causes new DNS answers to exclude that region after the documented TTL window; recovery restores it without manual route edits.
- Tailnet failover is tested independently from public failover and does not require exposing the Tailscale endpoints publicly.
- The legacy shared tailnet hostname, legacy DNS forwarder, and obsolete route aliases have no remaining tracked references when their cleanup phases are complete.

## Not included in this draft PR

- No live DNS changes or manual Flux reconciliation.
- No changes to the tailnet CoreDNS answers; they remain owner-pinned until a
  tailnet-only health-aware mechanism is selected.
- No changes to the public/private/Tailscale Gateway fleets beyond the
  standardized TSFlow route deployment.
- No Cloudflare Load Balancer.
- No BGP anycast or shared cross-region LoadBalancer IP.
- No replacement of the regional Tailscale endpoints with a single shared Tailscale Service.
- No assumption that the existing static k8gb Corefile already provides
  tailnet health failover.

## References

- [k8gb introduction and DNS GSLB model](https://github.com/k8gb-io/k8gb/blob/master/docs/intro.md)
- [k8gb LoadBalancer Service integration](https://www.k8gb.io/master/lbservice-integration/)
- [k8gb resource reference](https://www.k8gb.io/latest/resource_ref/)
- [Cilium Multi-Cluster Services API](https://docs.cilium.io/en/latest/network/clustermesh/mcsapi/)
- [Envoy Gateway backend routing](https://gateway.envoyproxy.io/v1.8/tasks/traffic/backend/)
- [Tailscale Kubernetes operator](https://tailscale.com/docs/kubernetes-operator/quickstart)
