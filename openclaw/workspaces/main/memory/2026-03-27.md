# Memory - 2026-03-27

## Daily Review (2026-03-27 10:00 UTC)

### Session Review
- Period: 2026-03-26 10:00 UTC to 2026-03-27 10:00 UTC
- Sessions analyzed:
  - `cron:daily-review` (dd867925) — yesterday's review
  - `cron:media-stack-health` (70dad7b7, 1ab6f1b4) — Ottawa media stack check, no errors
  - `cron:flux-health-check` (4f3bf504) — Flux health check, no errors
  - `cron:heartbeat` (77c1635f) — heartbeat, no errors
  - `cron:tailscale-network-health` (ee152942) — clean, all clusters healthy
  - `main Discord #kubernetes-manifests` (6193d4a7) — had errors (duplicate name key in YAML, git conflicts, EDITOR unset, Flux postBuild failures, woodpecker-server HelmRelease stalled)
  - `cron:cross-cluster-connectivity` (d005b25d) — ran cleanly; found `aperture` operator in ottawa, none in other clusters; `egressservices` CRD not found
- **Open PR already filed**: `workspace/git-kubectl-fixes-2026-03-26` — addresses git commit -m, kubectl --context placement, kustomize pre-flight, Flux suspend limitations
- No new breaking findings beyond what the open PR covers
- Workspace cross-reference: container names and mounts ✅

### New Gotchas Found (today)
1. **Tailscale operator deployment name is NOT "operator"** — in ottawa it is `deployment/aperture`; other clusters (robbinsdale, stpetersburg) have no separate operator deployment. Always discover with `kubectl get deployments -n tailscale-system` first.
2. **`egressservices` CRD does not exist** — `kubectl get egressservices -A` returns "the server doesn't have a resource type". This is not a permissions issue. The egressservices API is not installed in this cluster setup.
3. **`flux --context` accepts --context as a flag before subcommand** (e.g., `flux --context=ottawa get kustomization`), but `kubectl --context` must come after the resource verb. Yesterday's PR covers this.

### Pruning
- Oldest log: 2026-03-22 (5 days old, within 7-day window)
- No pruning needed

### Pending Issues (from prior logs, still unresolved)
1. **external-dns-unifi** — 401 Unauthorized from Unifi controller. API key expired/rotated. Needs new key from `https://192.168.50.1`
2. **Cloudflare DNS** — `status.rajsingh.info` A record still `10.50.10.14` (needs → `10.50.10.15`)
3. **Zot Public CDN GSLB** — `oci.cdn.keiretsu.top` has no DNS A record. k8gb healthyRecords empty because zot-public is ClusterIP
4. **userz-mcp.keiretsu.ts.net** — aperture operator logs show "host not found" for this MCP endpoint. Non-critical.

### Feedback Log
- No new corrections from Raj today
