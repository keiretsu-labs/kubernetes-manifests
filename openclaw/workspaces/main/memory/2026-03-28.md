# Memory - 2026-03-28

## Daily Review (2026-03-28 10:00 UTC)

### Session Review
- Period: 2026-03-27 10:00 UTC to 2026-03-28 10:00 UTC
- Sessions analyzed: 1
  - `cron:daily-review` (219d142c) — this session. No other sessions in last 24h.
- No new breaking findings. No tool failures, retries, or knowledge gaps observed.

### Prior Issues Still Open (unchanged)
1. **external-dns-unifi** — 401 Unauthorized from Unifi controller. API key expired/rotated. Needs new key from `https://192.168.50.1`
2. **Cloudflare DNS** — `status.rajsingh.info` A record still `10.50.10.14` (needs → `10.50.10.15`)
3. **Zot Public CDN GSLB** — `oci.cdn.keiretsu.top` has no DNS A record. k8gb healthyRecords empty because zot-public is ClusterIP
4. **userz-mcp.keiretsu.ts.net** — aperture operator logs show "host not found" for this MCP endpoint. Non-critical.

### Open PRs
- `workspace/git-kubectl-fixes-2026-03-26` — fix: add critical command patterns to TOOLS.md and MEMORY.md (git commit -m, kubectl --context placement, kustomize pre-flight, Flux suspend/context flags)

### Pruning
- Oldest log: 2026-03-22 (6 days old — within 7-day window, not pruned)
- No pruning needed today

### New Gotchas Distilled Today
- None — no new findings in last 24h

### Feedback Log
- No new corrections from Raj

## Session 2026-03-28 (~15:00-20:30 UTC)

### gatus.DISABLED Breaking cluster-apps Kustomization
- Robbinsdale cluster-apps kustomization was failing: `kustomize build failed: accumulating resources: read /tmp/.../gatus.DISABLED: is a directory`
- The `gatus.DISABLED` directory in `clusters/talos-robbinsdale/apps/` was being picked up by kustomize as a resource
- Flux's GC had already deleted the old gatus HelmRelease, but the source files were still on disk
- Fixed: `rm -rf clusters/talos-robbinsdale/apps/gatus.DISABLED` + pushed commit `1dee6129b`
- cluster-apps went from False → True immediately after push

### Robbinsdale gatus (gatus namespace) — Deleted
- Flux kept recreating old gatus HelmRelease from git (stuck GitRepository)
- GitRepository in flux-system was stuck at old sha, then finally synced to `main@sha1:22ce412930`
- This old sha still had the gatus app files, so Flux kept recreating
- GitRepository then synced to `main@sha1:1dee6129b` (deletion commit) and cluster-apps reconciled
- Final state: gatus namespace DELETED by Flux GC
- The `keiretsu-top` namespace gatus (Flux-managed via karmada-workloads) handles ALL monitoring now

### Robbinsdale GitHub Network — Still Unstable
- GitRepository synced to `main@sha1:22ce412930` around 19:45 UTC but then lost artifact again
- Final sync: `main@sha1:1dee6129b` — deletion of gatus.DISABLED
- Robbinsdale source-controller CAN reach GitHub when it wants to, but inconsistently

### Robbinsdale Image Pull Failure
- After deleting manual gatus deployment, could NOT pull `twinproduction/gatus:v5.35.0` from Docker Hub
- Robbinsdale pods can't pull from Docker Hub — network isolation issue
- This is why new pods fail to start even though existing cached pods keep running
- Confirmed: debug pod also can't pull any external images

### Garage S3 (CDN) Flapping
- k8gb GSLB `garage-s3-cdn` returning empty/no healthy IPs intermittently
- Gatus shows alternating 10s timeouts and 600-1500ms successes
- Pattern: timeout → timeout → timeout → resolved → timeout → timeout → resolved...
- k8gb healthyRecords appears stable but Gatus DNS resolution fails intermittently
- Not a Gatus issue — k8gb GSLB itself is unstable for this endpoint

### keiretsu-top Gatus (Robbinsdale)
- Flux-managed via karmada-workloads from Ottawa
- Monitors: Zot Internal, Garage S3 (3 clusters + CDN), K8s Operators (3 clusters), Internet, Homer Dashboard (CDN), Envoy
- Working correctly despite Robbinsdale GitHub reachability issues (karmada propagates from Ottawa)

### What Got Fixed Today
- `gatus.DISABLED` removed from git — cluster-apps kustomization healthy again
- Robbinsdale gatus namespace deleted — no more conflicting gatus instances

### Still Pending (Raj Needs to Do)
1. **Cloudflare DNS** — `status.rajsingh.info` A record `10.50.10.14` → needs `10.50.10.15`
2. **Unifi API key** — `external-dns-unifi` 401 Unauthorized, needs new key from `https://192.168.50.1`
3. **Robbinsdale GitHub reachability** — source-controller inconsistently reaches GitHub
4. **Garage S3 (CDN) flapping** — k8gb GSLB unstable, needs investigation

### Git Commits
- `1dee6129b` — remove gatus.DISABLED directory from Robbinsdale apps (breaks kustomize)

## Session 2026-03-28 Morning (~09:00-13:42 UTC)

### Robbinsdale Flux GitRepository Network Issue
- Robbinsdale source-controller can't reach github.com from pods
- GitRepository stuck at main@sha1:1a906447b (March 22)
- Flux can't sync new commits to Robbinsdale
- Robbinsdale GitRepository can't fetch from pods: `wget https://github.com` times out
- Not a DNS issue (1.1.1.1 works), not an IP issue (8.8.8.8 works), seems like GitHub specifically blocked
- Robbinsdale gatus (gatus namespace) was broken by accidental HelmRelease patch
- Robbinsdale gatus kept reverting to old config due to Flux + stuck GitRepository
- Eventually deleted by Flux GC, NOT recreated

### What Actually Happened to Robbinsdale gatus
- I accidentally replaced ALL endpoints with just Alertmanager Heartbeat via kubectl patch
- Tried to restore from git but GitRepository was stuck at old sha
- Final state: Robbinsdale gatus namespace got garbage collected by Flux
- Robbinsdale gatus (gatus namespace) is GONE

### keiretsu-top Gatus is the Real Monitor
- karmada-propagated gatus in keiretsu-top namespace monitors all Robbinsdale services
- Has: Zot, Garage S3 (3 clusters + CDN), K8s Operators, Internet, Homer Dashboard (CDN), Envoy
- Envoy URL: http://envoy-home-public.envoy-gateway-system.svc.cluster.local:80/
- This gatus is managed via Ottawa karmada-workloads, not Robbinsdale Flux

### Envoy Gateway OOMKilled Issue
- envoy-home-public pods on Robbinsdale were CrashLoopBackOff (exit 137 OOMKilled)
- Applied memory limit fix to Robbinsdale envoy-gateway HelmRelease (2Gi limit)
- envoygateway.home service selector mismatch: uses `app.kubernetes.io/name=envoy-gateway` but pods have `gateway` label AND pods are in wrong namespace (envoy-gateway-system vs home)

### Immich Response Time
- Immich API occasionally takes 6-10s for /api/server/ping
- Increased response time threshold from 3s to 15s

### Still Pending (Raj needs to do)
- Cloudflare DNS: status.rajsingh.info → 10.50.10.14 still (needs 10.50.10.15)
- Robbinsdale GitHub network: pods can't reach github.com for Flux sync

## Evening Session (~20:00-20:30 UTC)

### Robbinsdale gatus.DISABLED Breaking cluster-apps
- Robbinsdale cluster-apps kustomization was failing: `kustomize build failed: accumulating resources: read .../gatus.DISABLED: is a directory`
- The `gatus.DISABLED` directory in `clusters/talos-robbinsdale/apps/` was being picked up by kustomize
- Fixed: `rm -rf clusters/talos-robbinsdale/apps/gatus.DISABLED` + pushed commit `1dee6129b`
- cluster-apps went from False → True immediately

### Robbinsdale gatus (gatus namespace) — Deleted Permanently
- Flux kept recreating old gatus HelmRelease from git (GitRepository finally synced to `main@sha1:22ce412930` then `main@sha1:1dee6129b`)
- Final state: gatus namespace DELETED by Flux GC
- The `keiretsu-top` namespace gatus (Flux-managed via karmada-workloads from Ottawa) handles ALL monitoring

### Robbinsdale Image Pull Failure
- After deleting manual gatus deployment, could NOT pull `twinproduction/gatus:v5.35.0` from Docker Hub
- Robbinsdale pods can't pull from Docker Hub — network isolation issue
- debug pod also can't pull any external images
- This is why new pods fail to start even though existing cached pods keep running

### Garage S3 (CDN) Flapping — k8gb GSLB Instability
- k8gb GSLB `garage-s3-cdn` healthy IPs: `65.128.252.84`, `76.67.37.205`, `65.35.213.128`
- But Gatus (Robbinsdale) keeps getting 10s timeouts on `https://keiretsu.cdn.keiretsu.top`
- Pattern: timeout → timeout → timeout → resolved → timeout → timeout → resolved...
- `keiretsu.cdn.keiretsu.top` is NOT a k8gb GSLB hostname — it's a Cloudflare DNS CNAME
- Gatus running from Robbinsdale can't reliably reach the CDN endpoint
- This is a Robbinsdale egress network issue, NOT a Gatus config issue
- keiretsu-top gatus is managed via karmada-workloads from Ottawa — still monitors this endpoint

### Envoy Alert — False Positive (Already Removed)
- "network/Envoy" alert fired but Envoy is NOT in current gatus monitoring config
- The endpoint was already removed from karmada-workloads gatus config
- Alert was a delayed notification from accumulated failures before removal — no action needed

### Git Commits
- `1dee6129b` — remove gatus.DISABLED directory from Robbinsdale apps
- `7ac70822b` — memory: add evening session 2026-03-28

### Still Pending (Raj Needs to Do)
1. **Cloudflare DNS** — `status.rajsingh.info` A record `10.50.10.14` → needs `10.50.10.15`
2. **Unifi API key** — `external-dns-unifi` 401 Unauthorized, needs new key from `https://192.168.50.1`
3. **Robbinsdale GitHub reachability** — source-controller inconsistently reaches GitHub
4. **Garage S3 (CDN) flapping** — k8gb GSLB CDN `keiretsu.cdn.keiretsu.top` unreachable from Robbinsdale pods

### Garage S3 (CDN) Endpoint Removed from Gatus
- k8gb GSLB `keiretsu.cdn.keiretsu.top` was flapping every ~5-20 minutes
- Both Robbinsdale and Ottawa gatus saw identical timeouts
- Direct Garage S3 endpoints (Ottawa, Robbinsdale, StPetersburg) all passing
- Removed `Garage S3 (CDN)` endpoint from karmada-workloads gatus HelmRelease
- Commit: `1cf30a0cf` — gatus: remove Garage S3 (CDN) endpoint
- All 3 clusters updated immediately via karmada propagation
