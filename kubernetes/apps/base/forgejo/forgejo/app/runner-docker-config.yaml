---
apiVersion: v1
kind: Secret
metadata:
  name: forgejo-runner-docker-config
stringData:
  config.json: |
    {
      "auths": {
        "oci.cdn.keiretsu.top": {
          "username": "${OCI_USERNAME}",
          "password": "${OCI_PASSWORD}"
        }
      }
    }
