apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sheep-adder-tsflow-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block-replicated
  resources:
    requests:
      storage: 1Gi
