apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tailscale-pam
resources:
  - serve-config.yaml
  - postgres.yaml
  - statefulset.yaml
  - secret.sops.yaml
labels:
  - pairs:
      app.kubernetes.io/part-of: tailscale-pam
      app.kubernetes.io/component: lobby
