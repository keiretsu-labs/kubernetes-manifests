---
apiVersion: "cilium.io/v2"
kind: CiliumLoadBalancerIPPool
metadata:
  name: "k8s-pool"
spec:
  blocks:
    # The UniFi gateway is 10.73.0.254. Keep it out of LB-IPAM while
    # retaining the existing pool addresses, including 10.73.0.0 if used.
    - start: "10.73.0.0"
      stop: "10.73.0.253"
    - start: "10.73.0.255"
      stop: "10.73.255.254"
  serviceSelector:
    matchExpressions:
      - { key: somekey, operator: NotIn, values: ["never-used-value"] }
