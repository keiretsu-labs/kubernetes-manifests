---
apiVersion: v1
kind: Service
metadata:
  name: clustermesh-apiserver
  annotations:
    tailscale.com/hostname: cilium-mesh-api-stpetersburg
    tailscale.com/proxy-group: cilium-mesh-ingress
    tailscale.com/tags: "tag:k8s,tag:stpetersburg"
spec:
  clusterIP: 10.4.93.144
  type: LoadBalancer
  loadBalancerClass: tailscale
  externalTrafficPolicy: Cluster
  internalTrafficPolicy: Cluster
  sessionAffinity: ClientIP
  selector:
    k8s-app: clustermesh-apiserver
  ports:
    - name: etcd-client
      port: 2379
      targetPort: 2379
      protocol: TCP
