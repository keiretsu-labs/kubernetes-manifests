---
apiVersion: v1
kind: Service
metadata:
  name: holmes-shim
  labels:
    app.kubernetes.io/name: holmes-shim
    app.kubernetes.io/managed-by: ai-sre
spec:
  type: ClusterIP
  selector:
    app: holmes-shim
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
