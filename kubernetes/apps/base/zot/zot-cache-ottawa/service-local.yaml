---
# Local in-cluster alias so the Ottawa Zot registry reaches Dragonfly directly.
apiVersion: v1
kind: Service
metadata:
  name: zot-cache
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      protocol: TCP
      targetPort: 6379
  selector:
    app: dragonfly-zot
