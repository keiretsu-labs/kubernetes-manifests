---
# The public Prometheus-compatible endpoint is backed by central Mimir, so it
# is deliberately published only from Ottawa alongside the Mimir gateway.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prometheus-cdn
  namespace: keiretsu-top
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: public
      namespace: home
  hostnames:
    - "prometheus.cdn.${COMMON_DOMAIN}"
    - "prometheus.${COMMON_DOMAIN}"
  rules:
    # Prometheus runs in agent mode, so its local HTTP endpoint cannot answer
    # PromQL requests. Serve the federated Mimir API instead; all tenants are
    # available from the Ottawa primary, so callers see one coherent dataset.
    - matches:
        - path:
            type: PathPrefix
            value: /api/v1
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: mimir-gateway
          namespace: mimir
      filters:
        - type: RequestHeaderModifier
          requestHeaderModifier:
            set:
              - name: X-Forwarded-Proto
                value: https
              - name: X-Scope-OrgID
                value: talos-robbinsdale|talos-ottawa|talos-stpetersburg
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /prometheus/api/v1
    # Mimir deliberately has no Prometheus web UI. Send interactive users to
    # Grafana while preserving PromQL API compatibility above.
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            hostname: "grafana.${COMMON_DOMAIN}"
            port: 443
            statusCode: 302
---
apiVersion: k8gb.absa.oss/v1beta1
kind: Gslb
metadata:
  name: prometheus
  namespace: keiretsu-top
  annotations:
    k8gb.io/exposed-hostnames: "${LOCATION}.${COMMON_DOMAIN}"
spec:
  resourceRef:
    apiVersion: gateway.networking.k8s.io/v1
    kind: HTTPRoute
    name: prometheus-cdn
  strategy:
    type: roundRobin
    dnsTtlSeconds: 30
    weight:
      ottawa: 100
      robbinsdale: 0
      stpetersburg: 0
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: prometheus-tinyauth
  namespace: keiretsu-top
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: prometheus-cdn
  extAuth:
    headersToExtAuth:
      - cookie
      - x-forwarded-proto
      - x-forwarded-for
      - user-agent
    http:
      backendRefs:
        - name: tinyauth
          namespace: tinyauth
          port: 3000
      path: "/api/auth/envoy?path="
      headersToBackend:
        - remote-user
        - remote-email
        - remote-name
        - remote-groups
  authorization:
    defaultAction: Deny
    rules:
      - name: allow-raj-kartik
        action: Allow
        principal:
          headers:
            - name: Remote-Email
              values:
                - rajsinghcpre@gmail.com
                - kartikbharath@gmail.com
