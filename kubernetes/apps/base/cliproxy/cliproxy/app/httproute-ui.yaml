---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cliproxy-ui
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: public
      namespace: home
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: private
      namespace: home
  hostnames:
    - "cliproxy.${COMMON_DOMAIN}"
  rules:
    - matches:
        - path:
            type: Exact
            value: /management.html
        - path:
            type: PathPrefix
            value: /v0/management
        - path:
            type: PathPrefix
            value: /v0/resource/plugins
      backendRefs:
        - group: ""
          kind: Service
          name: cliproxy
          port: 8317
          weight: 1
      filters:
        - type: RequestHeaderModifier
          requestHeaderModifier:
            set:
              - name: X-Forwarded-Proto
                value: https
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            remove:
              - Server
              - X-XSS-Protection
            set:
              - name: X-Content-Type-Options
                value: nosniff
              - name: X-Frame-Options
                value: SAMEORIGIN
              - name: Cache-Control
                value: no-transform
---
# The Tailscale Gateway does not listen on *.keiretsu.top; it has a dedicated
# *.ts.keiretsu.top listener. Keep this separate so every parent/hostname pair
# is Accepted instead of leaving a partially-rejected UI route.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cliproxy-ui-ts
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: ts
      namespace: home
  hostnames:
    - "cliproxy.ts.${COMMON_DOMAIN}"
  rules:
    - matches:
        - path:
            type: Exact
            value: /management.html
        - path:
            type: PathPrefix
            value: /v0/management
        - path:
            type: PathPrefix
            value: /v0/resource/plugins
      backendRefs:
        - group: ""
          kind: Service
          name: cliproxy
          port: 8317
          weight: 1
      filters:
        - type: RequestHeaderModifier
          requestHeaderModifier:
            set:
              - name: X-Forwarded-Proto
                value: https
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            remove:
              - Server
              - X-XSS-Protection
            set:
              - name: X-Content-Type-Options
                value: nosniff
              - name: X-Frame-Options
                value: SAMEORIGIN
              - name: Cache-Control
                value: no-transform
