---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cliproxy-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block-replicated
  resources:
    requests:
      storage: 2Gi
