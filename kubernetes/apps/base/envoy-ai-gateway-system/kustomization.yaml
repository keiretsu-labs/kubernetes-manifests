---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml
  - ./install
  # routes/ applied by Flux Kustomization envoy-ai-gateway-system-routes
  # (targetNamespace: cliproxy). Do not include here — the install KS uses
  # targetNamespace: envoy-ai-gateway-system and would override cliproxy.
