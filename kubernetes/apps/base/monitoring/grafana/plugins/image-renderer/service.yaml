---
apiVersion: v1
kind: Service
metadata:
  name: grafana-image-renderer
  namespace: monitoring
spec:
  ports:
    - name: renderer
      port: 8081
      targetPort: renderer
      protocol: TCP
  selector:
    app: grafana-image-renderer
