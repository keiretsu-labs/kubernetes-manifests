---
apiVersion: v1
kind: Service
metadata:
  name: grafana-mcp
  labels:
    app.kubernetes.io/name: grafana-mcp
spec:
  selector:
    app.kubernetes.io/name: grafana-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
