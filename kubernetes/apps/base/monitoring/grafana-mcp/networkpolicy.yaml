---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: grafana-mcp-ingress
spec:
  description: Only the Bhaiya control plane or bhaiya-mcp edge may use the Grafana MCP listener; Prometheus and kubelet receive health-only access.
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: grafana-mcp
  ingress:
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: bhaiya
            app.kubernetes.io/name: bhaiya
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: bhaiya
            app.kubernetes.io/name: bhaiya-mcp
            app.kubernetes.io/component: mcp-edge
      toPorts:
        - ports:
            - port: "8000"
              protocol: TCP
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: monitoring
            app.kubernetes.io/name: prometheus-agent
            app.kubernetes.io/instance: kube-prometheus-stack
            operator.prometheus.io/name: kube-prometheus-stack
      toPorts:
        - ports:
            - port: "8000"
              protocol: TCP
          rules:
            http:
              - method: GET
                path: /metrics$
    - fromEntities:
        - host
        - remote-node
      toPorts:
        - ports:
            - port: "8000"
              protocol: TCP
          rules:
            http:
              - method: GET
                path: /healthz$
