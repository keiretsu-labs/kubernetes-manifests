---
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: omnibus-s3-store
  namespace: media
spec:
  configuration:
    destinationPath: s3://omnibus-postgres/${LOCATION}/
    endpointURL: http://${COMMON_S3_ENDPOINT}
    s3Credentials:
      accessKeyId:
        name: omnibus-s3-backup
        key: AWS_ACCESS_KEY_ID
      secretAccessKey:
        name: omnibus-s3-backup
        key: AWS_SECRET_ACCESS_KEY
    wal:
      maxParallel: 8
  instanceSidecarConfiguration:
    env:
      - name: AWS_DEFAULT_REGION
        value: garage
  retentionPolicy: "7d"
