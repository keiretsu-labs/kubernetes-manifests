---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookorbit-config
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block-replicated
  resources:
    requests:
      storage: 20Gi
