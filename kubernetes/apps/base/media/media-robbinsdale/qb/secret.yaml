---
apiVersion: v1
kind: Secret
metadata:
  name: qbittorrent-secret
type: Opaque
stringData:
  WIREGUARD_PRIVATE_KEY: ${QB_WIREGUARD_PRIVATE_KEY}
  WIREGUARD_PUBLIC_KEY: ${QB_WIREGUARD_PUBLIC_KEY}
  WIREGUARD_ENDPOINT_IP: ${QB_WIREGUARD_ENDPOINT_IP}
  UPDATER_PROTONVPN_EMAIL: ${QB_PROTONVPN_EMAIL}
  UPDATER_PROTONVPN_PASSWORD: ${QB_PROTONVPN_PASSWORD}