---
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: tracearr-s3-store
  namespace: media
spec:
  configuration:
    # Garage is federated across sites. Keep each CNPG cluster in its own
    # prefix so simultaneous backups cannot share a Barman catalog.
    destinationPath: s3://tracearr-postgres/${LOCATION}/
    endpointURL: http://${COMMON_S3_ENDPOINT}
    s3Credentials:
      accessKeyId:
        name: tracearr-s3-backup
        key: AWS_ACCESS_KEY_ID
      secretAccessKey:
        name: tracearr-s3-backup
        key: AWS_SECRET_ACCESS_KEY
  instanceSidecarConfiguration:
    env:
      - name: AWS_DEFAULT_REGION
        value: garage
  retentionPolicy: "30d"
