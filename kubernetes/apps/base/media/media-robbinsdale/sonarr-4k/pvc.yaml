---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-4k-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ${STORAGECLASS_DEFAULT}
