---
apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: common
spec:
  metrics:
    enable: true
    serviceMonitor:
      enable: true
  statefulSet:
    pod:
      priorityClassName: "infra-high"
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              tailscale.com/managed: "true"
          matchLabelKeys:
            - tailscale.com/parent-resource
---
apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: common-unstable
spec:
  metrics:
    enable: true
    serviceMonitor:
      enable: true
  statefulSet:
    pod:
      tailscaleContainer:
        image: tailscale/tailscale:unstable-v1.101.284
      priorityClassName: "infra-high"
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              tailscale.com/managed: "true"
          matchLabelKeys:
            - tailscale.com/parent-resource
---
apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: common-dev
spec:
  metrics:
    enable: true
    serviceMonitor:
      enable: true
  statefulSet:
    pod:
      tailscaleContainer:
        image: ghcr.io/rajsinghtech/tailscale/tailscale:tun-service-metrics-64ee27f3c@sha256:d0105dea7e624c1af85d1ac5c01bb597e2c1df0ce76304248875ef0964bc602a
      priorityClassName: "infra-high"
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              tailscale.com/managed: "true"
          matchLabelKeys:
            - tailscale.com/parent-resource
---
apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: common-accept-routes
spec:
  tailscale:
    acceptRoutes: true
  metrics:
    enable: true
    serviceMonitor:
      enable: true
  statefulSet:
    pod:
      priorityClassName: "infra-high"
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              tailscale.com/managed: "true"
          matchLabelKeys:
            - tailscale.com/parent-resource
---
apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: common-userspace
spec:
  statefulSet:
    pod:
      tailscaleContainer:
        env:
          - name: TS_USERSPACE
            value: "true"
        securityContext:
          privileged: false
          capabilities:
            drop:
              - ALL
      tailscaleInitContainer:
        securityContext:
          privileged: false
          capabilities:
            drop:
              - ALL
---
apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: k8s-proxy-l4-test
spec:
  statefulSet:
    pod:
      tailscaleContainer:
        image: ghcr.io/rajsinghtech/tailscale/k8s-proxy:81
  metrics:
    enable: true
    serviceMonitor:
      enable: true
