---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firefly-upload
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: ceph-block-replicated
  resources:
    requests:
      storage: 20Gi
