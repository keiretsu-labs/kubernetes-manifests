---
apiVersion: v1
kind: Service
metadata:
  name: firefly-mcp
  labels:
    app.kubernetes.io/name: firefly-mcp
spec:
  selector:
    app.kubernetes.io/name: firefly-mcp
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
