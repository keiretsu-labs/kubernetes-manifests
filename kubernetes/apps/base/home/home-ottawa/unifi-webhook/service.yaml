---
apiVersion: v1
kind: Service
metadata:
  name: unifi-webhook
  namespace: home
  labels:
    app: unifi-webhook
spec:
  selector:
    app: unifi-webhook
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
