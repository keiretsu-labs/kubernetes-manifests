apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-hermes-${COMMON_DOMAIN//./-}
spec:
  dnsNames:
    - '*.hermes.${COMMON_DOMAIN}'
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: keiretsu-top
  secretName: wildcard-hermes-${COMMON_DOMAIN//./-}
  usages:
    - digital signature
    - key encipherment