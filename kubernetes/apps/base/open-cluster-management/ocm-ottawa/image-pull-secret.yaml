---
apiVersion: v1
kind: Secret
metadata:
  name: open-cluster-management-image-pull-credentials
type: kubernetes.io/dockerconfigjson
data:
  # OCM hard-codes this Secret on its ServiceAccount even for public images.
  # An empty docker config preserves public pulls without inventing credentials.
  .dockerconfigjson: e30=
