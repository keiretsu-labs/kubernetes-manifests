apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: bhaiya
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: bhaiya
  filterTags:
    pattern: '^v[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$version'
  policy:
    semver:
      range: '>=0.1.0'
