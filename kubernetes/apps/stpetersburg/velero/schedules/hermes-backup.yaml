---
apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: hermes-backup
  namespace: velero-system
spec:
  schedule: "0 7 * * *"
  skipImmediately: true
  template:
    ttl: 168h
    includedNamespaces:
      - hermes
      - agents
    defaultVolumesToFsBackup: true