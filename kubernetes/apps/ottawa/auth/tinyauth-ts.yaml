apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app tinyauth-ts
  namespace: flux-system
spec:
  targetNamespace: tinyauth
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  dependsOn:
    - name: tinyauth
  path: ./kubernetes/apps/base/auth/tinyauth-ts
  prune: true
  sourceRef:
    kind: GitRepository
    name: kubernetes-manifests
  interval: 30m
  retryInterval: 1m
  timeout: 5m
