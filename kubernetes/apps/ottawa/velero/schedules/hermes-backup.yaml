---
apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: hermes-backup
  namespace: velero-system
spec:
  schedule: "0 7 * * *"
  template:
    ttl: 720h
    includedNamespaces:
      - hermes
      - agents
    defaultVolumesToFsBackup: true