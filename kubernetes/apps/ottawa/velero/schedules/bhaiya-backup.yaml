---
apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: bhaiya-backup
  namespace: velero-system
spec:
  schedule: "0 8 * * *"
  template:
    ttl: 720h
    includedNamespaces:
      - bhaiya
    defaultVolumesToFsBackup: true