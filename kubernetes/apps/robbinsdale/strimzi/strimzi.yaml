# PARKED — every line below is commented out, so this pointer renders to
# nothing and Strimzi/Kafka is NOT deployed on Robbinsdale, even though the
# namespace directory and its kustomization.yaml still list this file.
# Uncomment to re-enable; delete the directory to decommission. Kept as-is so
# the Kafka topology (KRaft, node pools, TLSRoutes on the private gateway) is
# not lost. The architecture diagram in README.md shows it in amber for this
# reason.
# ---
# apiVersion: kustomize.toolkit.fluxcd.io/v1
# kind: Kustomization
# metadata:
#   name: &app strimzi
#   namespace: flux-system
# spec:
#   targetNamespace: strimzi
#   commonMetadata:
#     labels:
#       app.kubernetes.io/name: *app
#   path: ./kubernetes/apps/base/strimzi/strimzi
#   prune: true
#   sourceRef:
#     kind: GitRepository
#     name: kubernetes-manifests
#   interval: 30m
#   retryInterval: 1m
#   timeout: 10m # Increased timeout for Kafka operator and cluster 