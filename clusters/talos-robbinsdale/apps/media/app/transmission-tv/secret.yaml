---
apiVersion: v1
kind: Secret
metadata:
  name: transmission-tv-secret
type: Opaque
stringData:
  WIREGUARD_PRIVATE_KEY: ${TRANSMISSION_TV_WIREGUARD_PRIVATE_KEY}
  WIREGUARD_PUBLIC_KEY: ${TRANSMISSION_TV_WIREGUARD_PUBLIC_KEY}
  WIREGUARD_ENDPOINT_IP: ${TRANSMISSION_TV_WIREGUARD_ENDPOINT_IP}
  UPDATER_PROTONVPN_EMAIL: ${QB_PROTONVPN_EMAIL}
  UPDATER_PROTONVPN_PASSWORD: ${QB_PROTONVPN_PASSWORD}
