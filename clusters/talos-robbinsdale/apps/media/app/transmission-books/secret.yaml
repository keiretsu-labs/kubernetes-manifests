---
apiVersion: v1
kind: Secret
metadata:
  name: transmission-books-secret
type: Opaque
stringData:
  WIREGUARD_PRIVATE_KEY: ${TRANSMISSION_BOOKS_WIREGUARD_PRIVATE_KEY}
  WIREGUARD_PUBLIC_KEY: ${TRANSMISSION_BOOKS_WIREGUARD_PUBLIC_KEY}
  WIREGUARD_ENDPOINT_IP: ${TRANSMISSION_BOOKS_WIREGUARD_ENDPOINT_IP}
  UPDATER_PROTONVPN_EMAIL: ${QB_PROTONVPN_EMAIL}
  UPDATER_PROTONVPN_PASSWORD: ${QB_PROTONVPN_PASSWORD}
