---
apiVersion: network.keiretsu.ts.net/v1alpha1
kind: ServiceIngress
metadata:
  name: keiretsu-test
  namespace: keiretsu-test
spec:
  selector:
    app.kubernetes.io/name: keiretsu-test
  ports:
  - name: http
    port: 80
  hostname: "${LOCATION}-keiretsu-test"
  tags: "tag:k8s,tag:${LOCATION}"
  proxyGroup: common-ingress
  global:
    enabled: true
    hostname: keiretsu-test
    tags: "tag:k8s"
    ports:
    - name: http
      port: 80
    proxyGroup: common-ingress
