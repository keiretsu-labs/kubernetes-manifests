# ==============================================================================
# COMMON PROBES - Internal services shared across all clusters
# These use cluster-local DNS and should work from within the cluster
# ==============================================================================

---
# Media services (shared across clusters with media namespace)
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: http-common-media-services
spec:
  module: http_2xx
  prober:
    url: blackbox-exporter.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        # Jellyfin stack
        - http://jellyfin.media:8096/health
        - http://jellystat.media:3000
        - http://jellyseerr.media:5055
        # Plex
        - http://plex.media:32400/identity
        # Radarr
        - http://radarr-4kremux.media:7878
        - http://radarr-4k.media:7878
        - http://radarr-1080p.media:7878
        - http://radarr-anime.media:7878
        # Sonarr
        - http://sonarr-4k.media:8989
        - http://sonarr-1080p.media:8989
        - http://sonarr-anime.media:8989
        # Other
        - http://prowlarr.media:9696
        - http://bazarr-4kremux.media:6767
        - http://bazarr-4k.media:6767
        - http://bazarr-1080p.media:6767
        - http://bazarr-anime.media:6767
  metricRelabelings:
    - targetLabel: service
      replacement: blackbox-exporter
      action: replace

---
# Immich services
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: http-common-immich
spec:
  module: http_2xx
  prober:
    url: blackbox-exporter.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - http://immich-server.immich:2283/api/server/ping
        - http://immich-machine-learning.immich:3003/health
  metricRelabelings:
    - targetLabel: service
      replacement: blackbox-exporter
      action: replace

---
# ==============================================================================
# CLUSTER-SPECIFIC PROBES - These vary by cluster
# These use ${CLUSTER_DOMAIN} variable substitution
# ==============================================================================

---
# Monitoring stack (prometheus, grafana, alertmanager)
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: http-monitoring-stack
spec:
  module: http_2xx
  prober:
    url: blackbox-exporter.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://prometheus.${CLUSTER_DOMAIN}
        - https://grafana.${CLUSTER_DOMAIN}
        - https://alertmanager.${CLUSTER_DOMAIN}
  metricRelabelings:
    - targetLabel: service
      replacement: blackbox-exporter
      action: replace

---
# GitOps tools
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: http-gitops-tools
spec:
  module: http_2xx
  prober:
    url: blackbox-exporter.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://argocd.${CLUSTER_DOMAIN}
  metricRelabelings:
    - targetLabel: service
      replacement: blackbox-exporter
      action: replace

---
# Garage and storage
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: http-storage-garage
spec:
  module: http_2xx
  prober:
    url: blackbox-exporter.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://garage.${CLUSTER_DOMAIN}
        - https://s3-gateway.${CLUSTER_DOMAIN}
  metricRelabelings:
    - targetLabel: service
      replacement: blackbox-exporter
      action: replace

---
# Development platforms
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: http-dev-platforms
spec:
  module: http_2xx
  prober:
    url: blackbox-exporter.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://coder.${CLUSTER_DOMAIN}
        - https://code.${CLUSTER_DOMAIN}
        - https://temporal.${CLUSTER_DOMAIN}
  metricRelabelings:
    - targetLabel: service
      replacement: blackbox-exporter
      action: replace

---
# External/public endpoints
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: http-external-endpoints
spec:
  module: http_2xx
  prober:
    url: blackbox-exporter.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://www.rajsingh.info
  metricRelabelings:
    - targetLabel: service
      replacement: blackbox-exporter
      action: replace
