---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: keiretsu-external-dns
spec:
  interval: 5m
  releaseName: keiretsu-external-dns
  chart:
    spec:
      chart: external-dns
      version: "1.20.0"
      sourceRef:
        kind: HelmRepository
        name: external-dns
        namespace: flux-system
      interval: 1h
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    serviceMonitor:
      enabled: true
    provider: cloudflare
    env:
      - name: CF_API_TOKEN
        valueFrom:
          secretKeyRef:
            name: cloudflare-keiretsu-com
            key: api-token
    extraArgs:
      - --gateway-label-filter=external-dns==keiretsu
    policy: sync
    sources:
      - gateway-httproute
      - gateway-tlsroute
      - gateway-tcproute
      - gateway-udproute
    domainFilters:
      - keiretsu.com
    txtOwnerId: ${LOCATION}-keiretsu-cdn
    txtPrefix: k8s.${LOCATION}.cdn.keiretsu.com.
