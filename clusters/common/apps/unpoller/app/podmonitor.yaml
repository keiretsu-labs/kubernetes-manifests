---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: unpoller
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: unpoller
  podMetricsEndpoints:
    - port: tcp
      interval: 2m
      scrapeTimeout: 30s
