---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflare-ddns-keiretsu-top
  labels:
    app: cloudflare-ddns-keiretsu-top
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflare-ddns-keiretsu-top
  template:
    metadata:
      labels:
        app: cloudflare-ddns-keiretsu-top
    spec:
      containers:
        - name: cloudflare-ddns
          image: favonia/cloudflare-ddns:latest
          env:
            - name: CLOUDFLARE_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflare
                  key: RAJSINGH_INFO_CLOUDFLARE_API_TOKEN
            - name: DOMAINS
              value: "${LOCATION}.keiretsu.top,gslb-ns-${LOCATION}-cdn.keiretsu.top"
            - name: PROXIED
              value: "false"
            - name: IP6_PROVIDER
              value: "none"
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
