# Network and system optimizations via sysctls
machine:
  sysctls:
    # Network optimizations
    net.core.default_qdisc: fq
    net.core.rmem_max: "67108864"
    net.core.wmem_max: "67108864"
    net.ipv4.tcp_congestion_control: bbr
    net.ipv4.tcp_fastopen: "3"
    net.ipv4.tcp_mtu_probing: "1"
    net.ipv4.tcp_rmem: "4096 87380 33554432"
    net.ipv4.tcp_wmem: "4096 65536 33554432"
    net.ipv4.tcp_window_scaling: "1"

    # Filesystem watchers (for monitoring tools)
    fs.inotify.max_user_watches: "1048576"
    fs.inotify.max_user_instances: "8192"

    # eBPF JIT for Cilium
    net.core.bpf_jit_enable: "1"

    # gVisor: needs unprivileged user namespace creation (overrides KSPP default of 0)
    user.max_user_namespaces: "11255"
    # Note: vm.nr_hugepages is set per-node — spark-0 reserves them for GPU/DB,
    # orin-0 has only 8GB RAM and leaves the default (0).
