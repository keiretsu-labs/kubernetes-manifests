# Node-specific configuration for spark-1 (DGX Spark, paired with spark-0).
# Same NVIDIA bits as spark-0 — kept in sync because both run the GB10 Blackwell GPU.
# NOTE: arm64.nobti is baked into the schematic kernel args (factory.talos.dev) because
# Talos v1.13 uses UKI which forbids runtime install.extraKernelArgs. See talconfig.yaml.
machine:
  nodeLabels:
    node.kubernetes.io/instance-type: dgx-spark
    nvidia.com/gpu.product: Blackwell
    nvidia.com/gpu.present: "true"
    # gpu-operator NFD-style labels — see spark-0.yaml for rationale
    feature.node.kubernetes.io/pci-10de.present: "true"
    feature.node.kubernetes.io/system-os_release.ID: ubuntu
    feature.node.kubernetes.io/system-os_release.VERSION_ID: "24.04"
    feature.node.kubernetes.io/system-os_release.VERSION_ID.major: "24"
    feature.node.kubernetes.io/cpu-model.vendor_id: NVIDIA
  sysctls:
    vm.nr_hugepages: "2048"
  network:
    interfaces:
      # ConnectX-7 RDMA rail to spark-0 (single 200G DAC link, /30 subnet)
      - interface: eth2
        mtu: 9000
        addresses:
          - 192.168.74.2/30
  kernel:
    modules:
      - name: nvidia
      - name: nvidia_uvm
      - name: nvidia_drm
        parameters:
          - modeset=1
          - fbdev=1
      - name: nvidia_modeset
      - name: ib_core
      - name: rdma_cm
      - name: rdma_ucm
  files:
    - content: |
        [plugins."io.containerd.grpc.v1.cri"]
          enable_unprivileged_ports = true
          enable_unprivileged_icmp = true
        [plugins."io.containerd.cri.v1.runtime"]
          [plugins."io.containerd.cri.v1.runtime".containerd]
            default_runtime_name = "nvidia"
      path: /etc/cri/conf.d/20-customization.part
      op: create
  udev:
    rules:
      - SUBSYSTEM=="drm", KERNEL=="renderD*", GROUP="44", MODE="0660"
      - KERNEL=="nvidia*", MODE="0666"
      - KERNEL=="nvidia-uvm*", MODE="0666"
