---
apiVersion: openclaw.rocks/v1alpha1
kind: OpenClawInstance
metadata:
  name: raj
spec:
  config:
    raw:
      models:
        mode: merge
        providers:
          openai:
            baseUrl: http://ai.raj.svc.cluster.local/v1
            auth: api-key
            models:
              - id: gpt-5.5
                name: gpt-5.5
              - id: Qwen3.6-27B
                name: Qwen3.6-27B
      agents:
        defaults:
          embeddedHarness:
            runtime: pi
            fallback: none
          model:
            primary: openai/gpt-5.5
            fallbacks:
              - openai/Qwen3.6-27B
      session:
        scope: per-sender
  envFrom:
    - secretRef:
        name: openclaw-api-keys
  env:
    - name: OPENAI_BASE_URL
      value: http://ai.raj.svc.cluster.local/v1
    - name: OPENCLAW_DISABLE_BONJOUR
      value: "1"
  storage:
    persistence:
      enabled: true
      storageClass: ceph-block-replicated
      size: 20Gi
  gateway:
    controlUiOrigins:
      - http://raj-assistant.keiretsu.ts.net
      - http://raj-assistant
  networking:
    service:
      type: ClusterIP
  security:
    networkPolicy:
      enabled: false
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      cpu: "2"
      memory: 4Gi
