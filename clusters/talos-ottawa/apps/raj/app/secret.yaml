---
apiVersion: v1
kind: Secret
metadata:
  name: openclaw-api-keys
type: Opaque
stringData:
  OPENAI_API_KEY: "${OPENCLAW_OPENAI_API_KEY:=aperture}"
  ANTHROPIC_API_KEY: "${OPENCLAW_ANTHROPIC_API_KEY:=}"
