---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# NOTE: Do not add namespace transformer here - the SOPS-encrypted secret
# has encrypted metadata which causes kustomize nameReference transformer to panic.
# The secret already has namespace encoded within it.
resources:
  - secret.sops.yaml
