---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# These resources are applied to the Karmada API server, NOT the host cluster.
# Use: kubectl --kubeconfig=/tmp/karmada.config apply -k .
resources:
  - namespace.yaml
  - deployment.yaml
  - propagation-policy.yaml
