---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fleet-tls
  namespace: fleet
spec:
  secretName: fleet
  issuerRef:
    name: killinit-cc
    kind: ClusterIssuer
  dnsNames:
    - "fleet.${CLUSTER_DOMAIN}"
