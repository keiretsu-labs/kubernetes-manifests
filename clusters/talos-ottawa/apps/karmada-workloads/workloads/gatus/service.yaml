---
apiVersion: v1
kind: Service
metadata:
  name: gatus
  namespace: keiretsu-top
  labels:
    app: gatus
spec:
  selector:
    app: gatus
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
