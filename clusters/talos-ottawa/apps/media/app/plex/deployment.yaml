---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: plex
  namespace: media
  labels:
    app: plex
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: plex
  template:
    metadata:
      labels:
        app: plex
    spec:
      resourceClaims:
        - name: gpu
          resourceClaimTemplateName: plex-gpu
      containers:
        - name: plex
          image: docker.io/plexinc/pms-docker:1.43.1.10611-1e34174b1
          resources:
            claims:
              - name: gpu
          imagePullPolicy: Always
          ports:
            - containerPort: 32400
              name: http
              protocol: TCP
          env:
            - name: TZ
              value: ${TIMEZONE}
            - name: VERSION
              value: docker
            - name: ADVERTISE_IP
              value: "https://plex.killinit.cc:443"
            - name: ALLOWED_NETWORKS
              value: "10.0.0.0/8,192.168.0.0/16"
          volumeMounts:
            - name: config
              mountPath: /config
            - name: media-volume
              mountPath: /media-share
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: plex-config  
        - name: media-volume
          persistentVolumeClaim:
            claimName: media-share