---
apiVersion: v1
kind: Service
metadata:
  name: kener
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kener
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
