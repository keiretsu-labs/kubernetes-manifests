---
apiVersion: v1
kind: Service
metadata:
  name: REPLACE_APP_NAME
  namespace: REPLACE_NAMESPACE
  labels:
    app: REPLACE_APP_NAME
spec:
  selector:
    app: REPLACE_APP_NAME
  ports:
    - port: 80
      targetPort: REPLACE_PORT
      protocol: TCP
